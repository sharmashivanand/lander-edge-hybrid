<?php

/*
add_shortcode('post_date', 'lander_post_date_shortcode');
add_shortcode('post_time', 'lander_post_time_shortcode');
add_shortcode('post_modified_date', 'lander_post_modified_date_shortcode');
add_shortcode('post_modified_time', 'lander_post_modified_time_shortcode');
add_shortcode('post_author', 'lander_post_author_shortcode');
add_shortcode('post_author_link', 'lander_post_author_link_shortcode');
add_shortcode('post_author_posts_link', 'lander_post_author_posts_link_shortcode');
add_shortcode('post_comments', 'lander_post_comments_shortcode');
add_shortcode('post_tags', 'lander_post_tags_shortcode');
add_shortcode('post_categories', 'lander_post_categories_shortcode');
add_shortcode('post_terms', 'lander_post_terms_shortcode');
add_shortcode('post_edit', 'lander_post_edit_shortcode');

add_shortcode('footer_backtotop', 'lander_footer_backtotop_shortcode');
add_shortcode('footer_copyright', 'lander_footer_copyright_shortcode');
add_shortcode('footer_childtheme_link', 'lander_footer_childtheme_link_shortcode');
add_shortcode('footer_lander_link', 'lander_footer_lander_link_shortcode');
add_shortcode('footer_binaryturf_link', 'lander_footer_binaryturf_link_shortcode');
add_shortcode('footer_wordpress_link', 'lander_footer_wordpress_link_shortcode');
add_shortcode('footer_site_title', 'lander_footer_site_title_shortcode');
add_shortcode('footer_home', 'lander_footer_home_shortcode');
add_shortcode('footer_loginout', 'lander_footer_loginout_shortcode');
*/

function lander_post_date_shortcode(){
    
    $defaults = array();

    $atts = shortcode_atts($defaults, $atts, 'shortcode_name');
    
    $output = '';
    
    return apply_filters('lander_post_date_shortcode', $output );
}

function lander_post_time_shortcode(){
    
    $defaults = array();

    $atts = shortcode_atts($defaults, $atts, 'shortcode_name');
    
    $output = '';
    
    return apply_filters('lander_post_time_shortcode', $output );
}

function lander_post_modified_date_shortcode(){
    
    $defaults = array();

    $atts = shortcode_atts($defaults, $atts, 'shortcode_name');
    
    $output = '';
    
    return apply_filters('lander_post_modified_date_shortcode', $output );
}

function lander_post_modified_time_shortcode(){
    
    $defaults = array();

    $atts = shortcode_atts($defaults, $atts, 'shortcode_name');
    
    $output = '';
    
    return apply_filters('lander_post_modified_time_shortcode', $output );
}

function lander_post_author_shortcode(){
    
    $defaults = array();

    $atts = shortcode_atts($defaults, $atts, 'shortcode_name');
    
    $output = '';
    
    return apply_filters('lander_post_author_shortcode', $output );
}

function lander_post_author_link_shortcode(){
    
    $defaults = array();

    $atts = shortcode_atts($defaults, $atts, 'shortcode_name');
    
    $output = '';
    
    return apply_filters('lander_post_author_link_shortcode', $output );
}

function lander_post_author_posts_link_shortcode(){
    
    $defaults = array();

    $atts = shortcode_atts($defaults, $atts, 'shortcode_name');
    
    $output = '';
    
    return apply_filters('lander_post_author_posts_link_shortcode', $output );
}

function lander_post_comments_shortcode(){
    
    $defaults = array();

    $atts = shortcode_atts($defaults, $atts, 'shortcode_name');
    
    $output = '';
    
    return apply_filters('lander_post_comments_shortcode', $output );
}

function lander_post_tags_shortcode(){
    
    $defaults = array();

    $atts = shortcode_atts($defaults, $atts, 'shortcode_name');
    
    $output = '';
    
    return apply_filters('lander_post_tags_shortcode', $output );
}

function lander_post_categories_shortcode(){
    
    $defaults = array();

    $atts = shortcode_atts($defaults, $atts, 'shortcode_name');
    
    $output = '';
    
    return apply_filters('lander_post_categories_shortcode', $output );
}

function lander_post_terms_shortcode(){
    
    $defaults = array();

    $atts = shortcode_atts($defaults, $atts, 'shortcode_name');
    
    $output = '';
    
    return apply_filters('lander_post_terms_shortcode', $output );
}

function lander_post_edit_shortcode(){
    
    $defaults = array();

    $atts = shortcode_atts($defaults, $atts, 'shortcode_name');
    
    $output = '';
    
    return apply_filters('lander_post_edit_shortcode', $output );
}

function lander_footer_backtotop_shortcode(){
    
    $defaults = array();

    $atts = shortcode_atts($defaults, $atts, 'shortcode_name');
    
    $output = '';
    
    return apply_filters('lander_footer_backtotop_shortcode', $output );
}

function lander_footer_copyright_shortcode(){
    
    $defaults = array();

    $atts = shortcode_atts($defaults, $atts, 'shortcode_name');
    
    $output = '';
    
    return apply_filters('lander_footer_copyright_shortcode', $output );
}

function lander_footer_childtheme_link_shortcode(){
    
    $defaults = array();

    $atts = shortcode_atts($defaults, $atts, 'shortcode_name');
    
    $output = '';
    
    return apply_filters('lander_footer_childtheme_link_shortcode', $output );
}

function lander_footer_lander_link_shortcode(){
    
    $defaults = array();

    $atts = shortcode_atts($defaults, $atts, 'shortcode_name');
    
    $output = '';
    
    return apply_filters('lander_footer_lander_link_shortcode', $output );
}

function lander_footer_binaryturf_link_shortcode(){
    
    $defaults = array();

    $atts = shortcode_atts($defaults, $atts, 'shortcode_name');
    
    $output = '';
    
    return apply_filters('lander_footer_binaryturf_link_shortcode', $output );
}

function lander_footer_wordpress_link_shortcode(){
    
    $defaults = array();

    $atts = shortcode_atts($defaults, $atts, 'shortcode_name');
    
    $output = '';
    
    return apply_filters('lander_footer_wordpress_link_shortcode', $output );
}

function lander_footer_site_title_shortcode(){
    
    $defaults = array();

    $atts = shortcode_atts($defaults, $atts, 'shortcode_name');
    
    $output = '';
    
    return apply_filters('lander_footer_site_title_shortcode', $output );
}

function lander_footer_home_shortcode(){
    
    $defaults = array();

    $atts = shortcode_atts($defaults, $atts, 'shortcode_name');
    
    $output = '';
    
    return apply_filters('lander_footer_home_shortcode', $output );
}

function lander_footer_loginout_shortcode(){
    
    $defaults = array();

    $atts = shortcode_atts($defaults, $atts, 'shortcode_name');
    
    $output = '';
    
    return apply_filters('lander_footer_loginout_shortcode', $output );
}
